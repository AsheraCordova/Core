//
//  Generated by the J2ObjC translator.  DO NOT EDIT!
//  source: D:\Java\git\core-javafx-widget\SWTAndroid\src\main\java\r\android\animation\ArgbEvaluator.java
//

#import <Foundation/Foundation.h>
#include "J2ObjC_header.h"

#pragma push_macro("INCLUDE_ALL_ArgbEvaluator")
#ifdef RESTRICT_ArgbEvaluator
#define INCLUDE_ALL_ArgbEvaluator 0
#else
#define INCLUDE_ALL_ArgbEvaluator 1
#endif
#undef RESTRICT_ArgbEvaluator

#if !defined (ADArgbEvaluator_) && (INCLUDE_ALL_ArgbEvaluator || defined(INCLUDE_ADArgbEvaluator))
#define ADArgbEvaluator_

#define RESTRICT_TypeEvaluator 1
#define INCLUDE_ADTypeEvaluator 1
#include "TypeEvaluator.h"

@class JavaLangFloat;

/*!
 @brief This evaluator can be used to perform type interpolation between integer
  values that represent ARGB colors.
 */
@interface ADArgbEvaluator : NSObject < ADTypeEvaluator >

#pragma mark Public

- (instancetype)init;

/*!
 @brief This function returns the calculated in-between value for a color
  given integers that represent the start and end values in the four
  bytes of the 32-bit int.Each channel is separately linearly interpolated
  and the resulting calculated values are recombined into the return value.
 @param fraction The fraction from the starting to the ending values
 @param startValue A 32-bit int value representing colors in the  separate bytes of the parameter
 @param endValue A 32-bit int value representing colors in the  separate bytes of the parameter
 @return A value that is calculated to be the linearly interpolated
  result, derived by separating the start and end values into separate
  color channels and interpolating each one separately, recombining the
  resulting values in the same way.
 */
- (id)evaluateWithFloat:(float)fraction
                 withId:(id)startValue
                 withId:(id)endValue;

/*!
 @brief Returns an instance of <code>ArgbEvaluator</code> that may be used in 
 <code>ValueAnimator.setEvaluator(TypeEvaluator)</code>.The same instance may
  be used in multiple <code>Animator</code>s because it holds no state.
 @return An instance of <code>ArgbEvalutor</code>.
 */
+ (ADArgbEvaluator *)getInstance;

@end

J2OBJC_STATIC_INIT(ADArgbEvaluator)

FOUNDATION_EXPORT void ADArgbEvaluator_init(ADArgbEvaluator *self);

FOUNDATION_EXPORT ADArgbEvaluator *new_ADArgbEvaluator_init(void) NS_RETURNS_RETAINED;

FOUNDATION_EXPORT ADArgbEvaluator *create_ADArgbEvaluator_init(void);

FOUNDATION_EXPORT ADArgbEvaluator *ADArgbEvaluator_getInstance(void);

J2OBJC_TYPE_LITERAL_HEADER(ADArgbEvaluator)

@compatibility_alias RAndroidAnimationArgbEvaluator ADArgbEvaluator;


#endif

#pragma pop_macro("INCLUDE_ALL_ArgbEvaluator")
