//
//  Generated by the J2ObjC translator.  DO NOT EDIT!
//  source: D:\Java\git\core-javafx-widget\SWTAndroid\src\main\java\r\android\view\ViewPropertyAnimator.java
//

#import <Foundation/Foundation.h>
#include "J2ObjC_header.h"

#pragma push_macro("INCLUDE_ALL_ViewPropertyAnimator")
#ifdef RESTRICT_ViewPropertyAnimator
#define INCLUDE_ALL_ViewPropertyAnimator 0
#else
#define INCLUDE_ALL_ViewPropertyAnimator 1
#endif
#undef RESTRICT_ViewPropertyAnimator

#if __has_feature(nullability)
#pragma clang diagnostic push
#pragma GCC diagnostic ignored "-Wnullability"
#pragma GCC diagnostic ignored "-Wnullability-completeness"
#endif

#if !defined (ADViewPropertyAnimator_) && (INCLUDE_ALL_ViewPropertyAnimator || defined(INCLUDE_ADViewPropertyAnimator))
#define ADViewPropertyAnimator_

@class ADView;
@class JavaLangBoolean;
@class JavaLangFloat;
@class JavaLangInteger;
@class JavaLangLong;
@class JavaUtilArrayList;
@protocol ADAnimator_AnimatorListener;
@protocol ADTimeInterpolator;
@protocol ADValueAnimator_AnimatorUpdateListener;
@protocol JavaLangRunnable;

/*!
 @brief This class enables automatic and optimized animation of select properties on View objects.
 If only one or two properties on a View object are being animated, then using an 
 <code>r.android.animation.ObjectAnimator</code> is fine; the property setters called by ObjectAnimator
  are well equipped to do the right thing to set the property and invalidate the view
  appropriately. But if several properties are animated simultaneously, or if you just want a
  more convenient syntax to animate a specific property, then ViewPropertyAnimator might be
  more well-suited to the task. 
 <p>This class may provide better performance for several simultaneous animations, because
  it will optimize invalidate calls to take place only once for several properties instead of each
  animated property independently causing its own invalidation. Also, the syntax of using this
  class could be easier to use because the caller need only tell the View object which
  property to animate, and the value to animate either to or by, and this class handles the
  details of configuring the underlying Animator class and starting it.</p>
  
 <p>This class is not constructed by the caller, but rather by the View whose properties
  it will animate. Calls to <code>r.android.view.View.animate()</code> will return a reference
  to the appropriate ViewPropertyAnimator object for that View.</p>
 */
@interface ADViewPropertyAnimator : NSObject {
 @public
  /*!
   @brief The View whose properties are being animated by this class.This is set at
  construction time.
   */
  ADView *mView_;
  /*!
   @brief This list holds the properties that have been asked to animate.We allow the caller to
  request several animations prior to actually starting the underlying animator.
   This
  enables us to run one single animator to handle several properties in parallel. Each
  property is tossed onto the pending list until the animation actually starts (which is
  done by posting it onto mView), at which time the pending list is cleared and the properties
  on that list are added to the list of properties associated with that animator.
   */
  JavaUtilArrayList *mPendingAnimations_;
}

#pragma mark Public

/*!
 @brief This method will cause the View's <code>alpha</code> property to be animated to the
  specified value.Animations already running on the property will be canceled.
 @param value The value to be animated to.
 - seealso: View#setAlpha(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)alphaWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>alpha</code> property to be animated by the
  specified value.Animations already running on the property will be canceled.
 @param value The amount to be animated by, as an offset from the current value.
 - seealso: View#setAlpha(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)alphaByWithFloat:(float)value;

/*!
 @brief Cancels all property animations that are currently running or pending.
 */
- (void)cancel;

/*!
 @brief Returns the current duration of property animations.If the duration was set on this
  object, that value is returned.
 Otherwise, the default value of the underlying Animator is returned.
 - seealso: #setDuration(long)
 @return The duration of animations, in milliseconds.
 */
- (int64_t)getDuration;

/*!
 @brief Returns the timing interpolator that this animation uses.
 @return The timing interpolator for this animation.
 */
- (id<ADTimeInterpolator>)getInterpolator;

/*!
 @brief Returns the current startDelay of property animations.If the startDelay was set on this
  object, that value is returned.
 Otherwise, the default value of the underlying Animator is returned.
 - seealso: #setStartDelay(long)
 @return The startDelay of animations, in milliseconds.
 */
- (int64_t)getStartDelay;

/*!
 @brief This method will cause the View's <code>rotation</code> property to be animated to the
  specified value.Animations already running on the property will be canceled.
 @param value The value to be animated to.
 - seealso: View#setRotation(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)rotationWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>rotation</code> property to be animated by the
  specified value.Animations already running on the property will be canceled.
 @param value The amount to be animated by, as an offset from the current value.
 - seealso: View#setRotation(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)rotationByWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>rotationX</code> property to be animated to the
  specified value.Animations already running on the property will be canceled.
 @param value The value to be animated to.
 - seealso: View#setRotationX(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)rotationXWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>rotationX</code> property to be animated by the
  specified value.Animations already running on the property will be canceled.
 @param value The amount to be animated by, as an offset from the current value.
 - seealso: View#setRotationX(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)rotationXByWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>rotationY</code> property to be animated to the
  specified value.Animations already running on the property will be canceled.
 @param value The value to be animated to.
 - seealso: View#setRotationY(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)rotationYWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>rotationY</code> property to be animated by the
  specified value.Animations already running on the property will be canceled.
 @param value The amount to be animated by, as an offset from the current value.
 - seealso: View#setRotationY(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)rotationYByWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>scaleX</code> property to be animated to the
  specified value.Animations already running on the property will be canceled.
 @param value The value to be animated to.
 - seealso: View#setScaleX(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)scaleXWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>scaleX</code> property to be animated by the
  specified value.Animations already running on the property will be canceled.
 @param value The amount to be animated by, as an offset from the current value.
 - seealso: View#setScaleX(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)scaleXByWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>scaleY</code> property to be animated to the
  specified value.Animations already running on the property will be canceled.
 @param value The value to be animated to.
 - seealso: View#setScaleY(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)scaleYWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>scaleY</code> property to be animated by the
  specified value.Animations already running on the property will be canceled.
 @param value The amount to be animated by, as an offset from the current value.
 - seealso: View#setScaleY(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)scaleYByWithFloat:(float)value;

/*!
 @brief Sets the duration for the underlying animator that animates the requested properties.
 By default, the animator uses the default value for ValueAnimator. Calling this method
  will cause the declared value to be used instead.
 @param duration The length of ensuing property animations, in milliseconds. The value  cannot be negative.
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)setDurationWithLong:(int64_t)duration;

/*!
 @brief Sets the interpolator for the underlying animator that animates the requested properties.
 By default, the animator uses the default interpolator for ValueAnimator. Calling this method
  will cause the declared object to be used instead.
 @param interpolator The TimeInterpolator to be used for ensuing property animations. A value  of 
  <code> null </code>  will result in linear interpolation.
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)setInterpolatorWithADTimeInterpolator:(id<ADTimeInterpolator>)interpolator;

/*!
 @brief Sets a listener for events in the underlying Animators that run the property
  animations.
 - seealso: Animator.AnimatorListener
 @param listener The listener to be called with AnimatorListener events. A value of  
  <code> null </code>  removes any existing listener.
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)setListenerWithADAnimator_AnimatorListener:(id<ADAnimator_AnimatorListener>)listener;

/*!
 @brief Sets the startDelay for the underlying animator that animates the requested properties.
 By default, the animator uses the default value for ValueAnimator. Calling this method
  will cause the declared value to be used instead.
 @param startDelay The delay of ensuing property animations, in milliseconds. The value  cannot be negative.
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)setStartDelayWithLong:(int64_t)startDelay;

/*!
 @brief Sets a listener for update events in the underlying ValueAnimator that runs
  the property animations.Note that the underlying animator is animating between
  0 and 1 (these values are then turned into the actual property values internally
  by ViewPropertyAnimator).
 So the animator cannot give information on the current
  values of the properties being animated by this ViewPropertyAnimator, although
  the view object itself can be queried to get the current values.
 - seealso: r.android.animation.ValueAnimator.AnimatorUpdateListener
 @param listener The listener to be called with update events. A value of  
  <code> null </code>  removes any existing listener.
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)setUpdateListenerWithADValueAnimator_AnimatorUpdateListener:(id<ADValueAnimator_AnimatorUpdateListener>)listener;

/*!
 @brief Starts the currently pending property animations immediately.Calling <code>start()</code>
  is optional because all animations start automatically at the next opportunity.
 However,
  if the animations are needed to start immediately and synchronously (not at the time when
  the next event is processed by the hierarchy, which is when the animations would begin
  otherwise), then this method can be used.
 */
- (void)start;

/*!
 @brief This method will cause the View's <code>translationX</code> property to be animated to the
  specified value.Animations already running on the property will be canceled.
 @param value The value to be animated to.
 - seealso: View#setTranslationX(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)translationXWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>translationX</code> property to be animated by the
  specified value.Animations already running on the property will be canceled.
 @param value The amount to be animated by, as an offset from the current value.
 - seealso: View#setTranslationX(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)translationXByWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>translationY</code> property to be animated to the
  specified value.Animations already running on the property will be canceled.
 @param value The value to be animated to.
 - seealso: View#setTranslationY(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)translationYWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>translationY</code> property to be animated by the
  specified value.Animations already running on the property will be canceled.
 @param value The amount to be animated by, as an offset from the current value.
 - seealso: View#setTranslationY(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)translationYByWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>translationZ</code> property to be animated to the
  specified value.Animations already running on the property will be canceled.
 @param value The value to be animated to.
 - seealso: View#setTranslationZ(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)translationZWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>translationZ</code> property to be animated by the
  specified value.Animations already running on the property will be canceled.
 @param value The amount to be animated by, as an offset from the current value.
 - seealso: View#setTranslationZ(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)translationZByWithFloat:(float)value;

/*!
 @brief Specifies an action to take place when the next animation ends.The action is only
  run if the animation ends normally; if the ViewPropertyAnimator is canceled during
  that animation, the runnable will not run.
 This method, along with <code>withStartAction(Runnable)</code>, is intended to help facilitate
  choreographing ViewPropertyAnimator animations with other animations or actions
  in the application. 
 <p>For example, the following code animates a view to x=200 and then back to 0:</p>
  @code

      Runnable endAction = new Runnable() {
          public void run() {
              view.animate().x(0);
          }
      };
      view.animate().x(200).withEndAction(endAction); 
  
@endcode
 @param runnable The action to run when the next animation ends.
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)withEndActionWithJavaLangRunnable:(id<JavaLangRunnable>)runnable;

/*!
 @brief The View associated with this ViewPropertyAnimator will have its 
 <code>layer type</code> set to 
 <code>View.LAYER_TYPE_HARDWARE</code> for the duration of the next animation.
 As stated in the documentation for <code>View.LAYER_TYPE_HARDWARE</code>,
  the actual type of layer used internally depends on the runtime situation of the
  view. If the activity and this view are hardware-accelerated, then the layer will be
  accelerated as well. If the activity or the view is not accelerated, then the layer will
  effectively be the same as <code>View.LAYER_TYPE_SOFTWARE</code>.
  
 <p>This state is not persistent, either on the View or on this ViewPropertyAnimator: the
  layer type of the View will be restored when the animation ends to what it was when this
  method was called, and this setting on ViewPropertyAnimator is only valid for the next
  animation. Note that calling this method and then independently setting the layer type of
  the View (by a direct call to <code>View.setLayerType(int, r.android.graphics.Paint)</code>) will
  result in some inconsistency, including having the layer type restored to its pre-withLayer()
  value when the animation ends.</p>
 - seealso: View#setLayerType(int, r.android.graphics.Paint)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)withLayer;

/*!
 @brief Specifies an action to take place when the next animation runs.If there is a 
 <code>startDelay</code> set on this ViewPropertyAnimator, then the
  action will run after that startDelay expires, when the actual animation begins.
 This method, along with <code>withEndAction(Runnable)</code>, is intended to help facilitate
  choreographing ViewPropertyAnimator animations with other animations or actions
  in the application.
 @param runnable The action to run when the next animation starts.
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)withStartActionWithJavaLangRunnable:(id<JavaLangRunnable>)runnable;

/*!
 @brief This method will cause the View's <code>x</code> property to be animated to the
  specified value.Animations already running on the property will be canceled.
 @param value The value to be animated to.
 - seealso: View#setX(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)xWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>x</code> property to be animated by the
  specified value.Animations already running on the property will be canceled.
 @param value The amount to be animated by, as an offset from the current value.
 - seealso: View#setX(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)xByWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>y</code> property to be animated to the
  specified value.Animations already running on the property will be canceled.
 @param value The value to be animated to.
 - seealso: View#setY(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)yWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>y</code> property to be animated by the
  specified value.Animations already running on the property will be canceled.
 @param value The amount to be animated by, as an offset from the current value.
 - seealso: View#setY(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)yByWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>z</code> property to be animated to the
  specified value.Animations already running on the property will be canceled.
 @param value The value to be animated to.
 - seealso: View#setZ(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)zWithFloat:(float)value;

/*!
 @brief This method will cause the View's <code>z</code> property to be animated by the
  specified value.Animations already running on the property will be canceled.
 @param value The amount to be animated by, as an offset from the current value.
 - seealso: View#setZ(float)
 @return This object, allowing calls to methods in this class to be chained.
 */
- (ADViewPropertyAnimator *)zByWithFloat:(float)value;

#pragma mark Package-Private

/*!
 @brief Constructor, called by View.This is private by design, as the user should only
  get a ViewPropertyAnimator by calling View.animate().
 @param view The View associated with this ViewPropertyAnimator
 */
- (instancetype)initWithADView:(ADView *)view;

- (id<ADAnimator_AnimatorListener>)getListener;

- (id<ADValueAnimator_AnimatorUpdateListener>)getUpdateListener;

- (bool)hasActions;

// Disallowed inherited constructors, do not use.

- (instancetype)init NS_UNAVAILABLE;

@end

J2OBJC_EMPTY_STATIC_INIT(ADViewPropertyAnimator)

J2OBJC_FIELD_SETTER(ADViewPropertyAnimator, mView_, ADView *)
J2OBJC_FIELD_SETTER(ADViewPropertyAnimator, mPendingAnimations_, JavaUtilArrayList *)

/*!
 @brief Constants used to associate a property being requested and the mechanism used to set
  the property (this class calls directly into View to set the properties in question).
 */
inline int32_t ADViewPropertyAnimator_get_NONE(void);
#define ADViewPropertyAnimator_NONE 0
J2OBJC_STATIC_FIELD_CONSTANT(ADViewPropertyAnimator, NONE, int32_t)

inline int32_t ADViewPropertyAnimator_get_TRANSLATION_X(void);
#define ADViewPropertyAnimator_TRANSLATION_X 1
J2OBJC_STATIC_FIELD_CONSTANT(ADViewPropertyAnimator, TRANSLATION_X, int32_t)

inline int32_t ADViewPropertyAnimator_get_TRANSLATION_Y(void);
#define ADViewPropertyAnimator_TRANSLATION_Y 2
J2OBJC_STATIC_FIELD_CONSTANT(ADViewPropertyAnimator, TRANSLATION_Y, int32_t)

inline int32_t ADViewPropertyAnimator_get_TRANSLATION_Z(void);
#define ADViewPropertyAnimator_TRANSLATION_Z 4
J2OBJC_STATIC_FIELD_CONSTANT(ADViewPropertyAnimator, TRANSLATION_Z, int32_t)

inline int32_t ADViewPropertyAnimator_get_SCALE_X(void);
#define ADViewPropertyAnimator_SCALE_X 8
J2OBJC_STATIC_FIELD_CONSTANT(ADViewPropertyAnimator, SCALE_X, int32_t)

inline int32_t ADViewPropertyAnimator_get_SCALE_Y(void);
#define ADViewPropertyAnimator_SCALE_Y 16
J2OBJC_STATIC_FIELD_CONSTANT(ADViewPropertyAnimator, SCALE_Y, int32_t)

inline int32_t ADViewPropertyAnimator_get_ROTATION(void);
#define ADViewPropertyAnimator_ROTATION 32
J2OBJC_STATIC_FIELD_CONSTANT(ADViewPropertyAnimator, ROTATION, int32_t)

inline int32_t ADViewPropertyAnimator_get_ROTATION_X(void);
#define ADViewPropertyAnimator_ROTATION_X 64
J2OBJC_STATIC_FIELD_CONSTANT(ADViewPropertyAnimator, ROTATION_X, int32_t)

inline int32_t ADViewPropertyAnimator_get_ROTATION_Y(void);
#define ADViewPropertyAnimator_ROTATION_Y 128
J2OBJC_STATIC_FIELD_CONSTANT(ADViewPropertyAnimator, ROTATION_Y, int32_t)

inline int32_t ADViewPropertyAnimator_get_X(void);
#define ADViewPropertyAnimator_X 256
J2OBJC_STATIC_FIELD_CONSTANT(ADViewPropertyAnimator, X, int32_t)

inline int32_t ADViewPropertyAnimator_get_Y(void);
#define ADViewPropertyAnimator_Y 512
J2OBJC_STATIC_FIELD_CONSTANT(ADViewPropertyAnimator, Y, int32_t)

inline int32_t ADViewPropertyAnimator_get_Z(void);
#define ADViewPropertyAnimator_Z 1024
J2OBJC_STATIC_FIELD_CONSTANT(ADViewPropertyAnimator, Z, int32_t)

inline int32_t ADViewPropertyAnimator_get_ALPHA(void);
#define ADViewPropertyAnimator_ALPHA 2048
J2OBJC_STATIC_FIELD_CONSTANT(ADViewPropertyAnimator, ALPHA, int32_t)

FOUNDATION_EXPORT void ADViewPropertyAnimator_initWithADView_(ADViewPropertyAnimator *self, ADView *view);

FOUNDATION_EXPORT ADViewPropertyAnimator *new_ADViewPropertyAnimator_initWithADView_(ADView *view) NS_RETURNS_RETAINED;

FOUNDATION_EXPORT ADViewPropertyAnimator *create_ADViewPropertyAnimator_initWithADView_(ADView *view);

J2OBJC_TYPE_LITERAL_HEADER(ADViewPropertyAnimator)

@compatibility_alias RAndroidViewViewPropertyAnimator ADViewPropertyAnimator;


#endif

#if !defined (ADViewPropertyAnimator_NameValuesHolder_) && (INCLUDE_ALL_ViewPropertyAnimator || defined(INCLUDE_ADViewPropertyAnimator_NameValuesHolder))
#define ADViewPropertyAnimator_NameValuesHolder_

@class JavaLangFloat;
@class JavaLangInteger;

/*!
 @brief This is the information we need to set each property during the animation.
 mNameConstant is used to set the appropriate field in View, and the from/delta
  values are used to calculate the animated value for a given animation fraction
  during the animation.
 */
@interface ADViewPropertyAnimator_NameValuesHolder : NSObject {
 @public
  int32_t mNameConstant_;
  float mFromValue_;
  float mDeltaValue_;
}

#pragma mark Package-Private

- (instancetype)initWithInt:(int32_t)nameConstant
                  withFloat:(float)fromValue
                  withFloat:(float)deltaValue;

// Disallowed inherited constructors, do not use.

- (instancetype)init NS_UNAVAILABLE;

@end

J2OBJC_EMPTY_STATIC_INIT(ADViewPropertyAnimator_NameValuesHolder)

FOUNDATION_EXPORT void ADViewPropertyAnimator_NameValuesHolder_initWithInt_withFloat_withFloat_(ADViewPropertyAnimator_NameValuesHolder *self, int32_t nameConstant, float fromValue, float deltaValue);

FOUNDATION_EXPORT ADViewPropertyAnimator_NameValuesHolder *new_ADViewPropertyAnimator_NameValuesHolder_initWithInt_withFloat_withFloat_(int32_t nameConstant, float fromValue, float deltaValue) NS_RETURNS_RETAINED;

FOUNDATION_EXPORT ADViewPropertyAnimator_NameValuesHolder *create_ADViewPropertyAnimator_NameValuesHolder_initWithInt_withFloat_withFloat_(int32_t nameConstant, float fromValue, float deltaValue);

J2OBJC_TYPE_LITERAL_HEADER(ADViewPropertyAnimator_NameValuesHolder)


#endif


#if __has_feature(nullability)
#pragma clang diagnostic pop
#endif
#pragma pop_macro("INCLUDE_ALL_ViewPropertyAnimator")
